type: object
properties:
  tox:
    type: object
    properties:
      testenv:
        type: array
        items:
          type: string
        example: 'cover'
      tox_file:
        type: string
        default: 'tox.ini'
        example: 'tox.ini'
    required:
      - testenv

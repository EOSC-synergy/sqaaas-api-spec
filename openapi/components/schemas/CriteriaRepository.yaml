type: object
properties:
  '[a-z0-9_-]*':
    type: object
    properties:
      container:
        type: string
      additionalProperties:
        oneOf:
          - $ref: ../schemas/Command.yaml
          - $ref: ../schemas/Tox.yaml

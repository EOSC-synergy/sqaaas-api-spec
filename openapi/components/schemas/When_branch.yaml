type: object
properties:
  pattern:
    type: string
    minLength: 1
    example: 'release-\\d+'
  comparator:
    type: string
    enum: [EQUALS, GLOB, REGEXP]
    default: GLOB
required:
  - pattern

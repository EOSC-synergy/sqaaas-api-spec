type: object
properties:
  type:
    type: string
    enum: [docker_compose]
    description: Orchestration technology to deploy the pipeline stages
  name:
    type: string
    description: Container name
  template:
    type: string
    description: Relative path (in the repository) to the container definition

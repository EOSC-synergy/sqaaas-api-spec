type: object
description: Details about Python's tox configuration
properties:
  testenv:
    type: array
    items:
      type: string
      minLength: 1
    default: ['ALL']
    example: ['cover']
  tox_file:
    type: string
    default: 'tox.ini'
    example: 'tox.ini'

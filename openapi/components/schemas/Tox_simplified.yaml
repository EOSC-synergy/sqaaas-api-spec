type: object
properties:
  testenv:
    type: array
    items:
      type: string
      minLength: 1
    minItems: 1
    example: ['cover']
  tox_file:
    type: string
    default: 'tox.ini'
    example: 'tox.ini'
required:
  - testenv

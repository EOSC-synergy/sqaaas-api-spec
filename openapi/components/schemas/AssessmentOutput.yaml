type: object
properties:
  report:
    type: object
    additionalProperties:
      description: ID of the criterion
      type: object
      properties:
        valid:
          type: boolean
          default: false
        data:
          type: object
          properties:
            REQUIRED:
              type: array
              items:
                $ref: './AssessmentOutputTool.yaml'
            RECOMMENDED:
              type: array
              items:
                $ref: './AssessmentOutputTool.yaml'
            OPTIONAL:
              type: array
              items:
                $ref: './AssessmentOutputTool.yaml'
  badge:
    type: object
    properties:
      software:
        $ref: './Badge.yaml'
      services:
        $ref: './Badge.yaml'
      fair:
        $ref: './Badge.yaml'

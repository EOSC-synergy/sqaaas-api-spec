type: object
properties:
  report:
    type: object
    additionalProperties:
      description: ID of the criterion
      type: object
      properties:
        valid:
          type: boolean
          default: false
        validator_data:
          type: array
          items:
            $ref: './AssessmentOutputValidator.yaml'
  badge:
    type: object
    properties:
      software:
        $ref: './Badge.yaml'
      services:
        $ref: './Badge.yaml'
      fair:
        $ref: './Badge.yaml'

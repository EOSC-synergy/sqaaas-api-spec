type: object
properties:
  report:
    type: object
    additionalProperties:
      description: ID of the criterion
      type: object
      properties:
        valid:
          type: boolean
          default: false
        subcriteria:
          type: object
          additionalProperties:
            $ref: './AssessmentOutputSubcriteria.yaml'
  badge:
    type: object
    properties:
      software:
        $ref: './Badge.yaml'
      services:
        $ref: './Badge.yaml'
      fair:
        $ref: './Badge.yaml'

type: object
properties:
  report:
    type: object
    additionalProperties:
      type: object
      additionalProperties:
        type: object
        properties:
          valid:
            type: boolean
            default: false
          data:
            type: object
            properties:
              REQUIRED:
                type: array
                items:
                  type: object
                  additionalProperties:
                    type: string
              RECOMMENDED:
                type: array
                items:
                  type: object
                  additionalProperties:
                    type: string
              OPTIONAL:
                type: array
                items:
                  type: object
                  additionalProperties:
                    type: string
  badge:
    type: object
    properties:
      software:
        type: string
        enum: [BRONZE, SILVER, GOLD]
      services:
        type: string
        enum: [BRONZE, SILVER, GOLD]
      fair:
        type: string
        enum: [BRONZE, SILVER, GOLD]
example:
  report:
    QC.Lic:
      valid: true
      data:
        REQUIRED:
          - licensee: "<stdout>"
  badge:
    software: "BRONZE"
    services: "SILVER"
    fair: "GOLD"

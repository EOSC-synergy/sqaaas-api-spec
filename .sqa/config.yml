config:
  credentials:
      - id: orviz-github-basic
        username_var: GIT_USER
        password_var: GIT_PASSWORD

sqa_criteria:
  qc_doc:
    repos:
      sqaaas-api-spec:
        container: node
        commands:
          - /sqaaas-api-spec/.git_credential_helper.sh get
          - npm --prefix /sqaaas-api-spec install
          - npm --prefix /sqaaas-api-spec run build
          - npm --prefix /sqaaas-api-spec run deploy -- --user="Pablo Orviz <orviz@ifca.unican.es>" --repo=https://orviz@github.com/eosc-synergy/sqaaas-api-spec
